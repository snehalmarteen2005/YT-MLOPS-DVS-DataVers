1.create git repo and close it in local .
2.create code.py file and add code to it .(it wil save a csv file to a new "data" folder)
3.do a git add-commit push before initializing dvc.
#pip install dvc
4.how we do "dvc init" (creates , dvcignore ,.dvc)
5. now do "mkdir s3" (create a new s3 directory)
6. how we do "dvc remote add -d myremote s3"
7.next " dvc add data/"
now it will ask to do : ("git rm -r --cached 'data' "and "git commit)
8.Again we do "dvc add data/" (create data.dvc)then "git add .gitignore data.dvc"
9.now  "dvc commit" and then "dvc push"
 then do git add commit push to mark the stage as the first version of data 
10.now make changes to mycode.py to append a new row in data, check changes in the "dvc status"
11. again - - "dvc commit" and then "dvc push"
12.then git add -commit push (we are saving v2 of our data at this point)
13.check dvc/git status, everything should be upto date.
14.now repeat step 10-12 for v3 of data